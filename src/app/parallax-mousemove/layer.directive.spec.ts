import { LayerDirective } from './layer.directive';

describe('LayerDirective', () => {
  it('should create an instance', () => {
    const directive = new LayerDirective();
    expect(directive).toBeTruthy();
  });
});
