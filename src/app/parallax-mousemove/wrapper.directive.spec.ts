import { WrapperDirective } from './wrapper.directive';

describe('WrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new WrapperDirective();
    expect(directive).toBeTruthy();
  });
});
